{
  "permissions": {
    "allow": [
      "Bash(php artisan *)",
      "Bash(composer *)",
      "Bash(npm *)",
      "Bash(./vendor/bin/sail *)",
      "Bash(git add *)",
      "Bash(git commit *)",
      "Bash(git push *)",
      "Bash(git checkout *)",
      "Bash(git branch *)",
      "Bash(git merge *)",
      "Bash(git log *)",
      "Bash(git diff *)",
      "Bash(git status)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json; d=json.load(sys.stdin); print(d.get('tool_input',{}).get('file_path',''))\" | { read -r f; [[ \"$f\" == *.php ]] || exit 0; rel=\"${f#$(pwd)/}\"; if ! docker ps --format '{{.Names}}' 2>/dev/null | grep -q 'laravel.test'; then echo '{\"systemMessage\": \"Sail が起動していません。./vendor/bin/sail up -d で起動してください。\"}'; exit 1; fi; ./vendor/bin/sail exec -T laravel.test ./vendor/bin/pint \"$rel\"; }",
            "timeout": 30,
            "statusMessage": "Pint でフォーマット中..."
          }
        ]
      }
    ]
  }
}
